# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude Code local settings
.claude/settings.local.json
.claude/settings.local.json.tmp.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Regenerable lightmap bake masters — ship the KTX2, not these
# (tools/blender/build_map.py + `pnpm assets:lightmap` regenerate them)
assets/maps/*/lightmap.exr
assets/maps/*/lightmap_ldr.png

opencode.jsonc

# Rust build artifacts
sim/pkg/
server/target/
sim/target/
target/

# Environment variables (contains secrets)
.env

# Docker volume mounts
data/

secrets/

# modelview CLI output (regenerable, never committed)
.modelview/